# 4.1 의존성 — Phase 3 와 동일하게 순수 neo4j 드라이버만 쓴다.
#
# 이 토픽은 개념 지도(Method Map)라 무거운 검색기를 구현하지 않는다.
# mini_graph_neo4j.py 가 초소형 예시 그래프 위에서 Local/Path/Global 대표 Cypher 를
# 돌려 보여 주는 게 전부라, 의존성도 드라이버 한 줄로 끝난다.
# routing_demo.py 는 표준 라이브러리만 쓰므로 추가 설치가 없다(LLM 없이 동작).
neo4j>=5.26,<6
