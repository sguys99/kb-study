# 기본 경로(mock 추출 + 검증)는 pydantic 만 있으면 키 없이 돈다.
pydantic>=2

# ── 선택 의존 (LLM 백엔드를 쓸 때만) ──────────────────────────────────────────
# anthropic 백엔드:  ANTHROPIC_API_KEY 필요
#   anthropic>=0.40
# instructor 백엔드: ANTHROPIC_API_KEY 필요
#   instructor>=1.5
#   anthropic>=0.40
#
# 비용 대안: LLM 을 Ollama 로컬 모델로 바꿔도 파이프라인은 동일하다(품질만 차이).
#   instructor 는 OpenAI 호환 엔드포인트로 Ollama 를 붙일 수 있다.
