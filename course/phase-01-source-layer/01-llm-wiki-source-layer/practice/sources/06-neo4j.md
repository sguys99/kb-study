# Neo4j

Neo4j는 속성 그래프(Labeled Property Graph, LPG) 모델을 쓰는 그래프 데이터베이스다.
노드와 관계에 속성을 붙이고, Cypher 질의어로 경로를 탐색한다.

GraphRAG 계열은 추출한 엔티티와 관계를 어딘가에 저장해야 한다. 이 과정에서는 Neo4j를 그 저장소로 쓴다.
Neo4j는 네이티브 벡터 인덱스와 풀텍스트 인덱스를 함께 제공해 하이브리드 검색에 유리하다.

LightRAG는 그래프 저장 백엔드로 Neo4j를 붙일 수 있다. 즉 두 도구는 함께 쓰인다.
