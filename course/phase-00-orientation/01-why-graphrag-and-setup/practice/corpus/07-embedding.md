# 임베딩과 VoyageAI

임베딩은 텍스트를 벡터로 바꿔 의미 유사도를 잴 수 있게 한다. RAG의 벡터 검색이 이 위에서 돈다.

이 과정의 기본 임베딩은 VoyageAI의 voyage-3.5다. 기본 차원은 1024다.
비용이 부담되면 로컬 모델 bge-m3로 바꿔도 파이프라인은 동일하게 돈다.

임베딩만으로는 문서 사이의 명시적 관계를 표현하지 못한다.
"A가 B에서 파생됐다" 같은 관계는 벡터 거리로 드러나지 않는다. 그래프가 필요한 이유다.
