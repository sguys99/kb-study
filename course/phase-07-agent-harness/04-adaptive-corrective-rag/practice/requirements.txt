# 04-adaptive-corrective-rag — Router + Retrieval Grader + Query Rewrite 제어 계층.
#
# 이 토픽은 01~03 practice(Registry·3도구)를 그 위에 얹어 재사용한다.
# 새 무거운 의존은 없다. 기본 경로는 표준 라이브러리만으로 돈다(키 없이 mock).
#
# 설치:
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#
# 01~03 이 이미 쓰던 것(같은 가상환경을 재사용하면 이미 깔려 있다):
#   - anthropic  : LLM 경로(Router/Grader/Rewrite 판정). 키 없으면 mock 으로 폴백.
#   - pydantic   : 03 ontology_check 가 사용. 삼중항/판정 결과 구조화에 재사용.
#   - PyYAML     : 03 ontology.yaml 로더.
#
# anthropic·pydantic·PyYAML 만 있으면 이 토픽은 추가 설치 없이 돈다.
anthropic>=0.40
pydantic>=2.7
PyYAML>=6.0
