# 06 의존성 — 04/05 와 동일하게 순수 neo4j 드라이버만 쓴다.
#
# GDS 전용 파이썬 클라이언트(graphdatascience)는 일부러 쓰지 않는다.
# 04/05 가 GDS·인덱스·가드를 모두 neo4j 드라이버로 Cypher 프로시저를 직접 호출해 다뤘고,
# 06 도 같은 방식(CALL gds.* ... YIELD ...)으로 일관성을 유지한다. 의존성도 한 줄로 끝난다.
neo4j>=5.26,<6
